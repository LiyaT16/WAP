```JavaScript

console.log("start");

setTimeout(() => console.log("timeout 0"), 0);

setImmediate(() => console.log("immediate"));

process.nextTick(() => console.log("nextTick"));

Promise.resolve().then(() => console.log("promise"));

console.log("end");
1.
start
end // because they are synchronous
nextTick // has priority before promise microtask
promise // runs after nexttick
timeout 0 // in the even loop timer first then check
immediate

2.
immediate inside I/O
timeout inside I/O
-> The loop starts from timer but fs.readFile() callback happen inside poll. then the next phase is the check room so setImmediate will run then it will back to timer then setTimeout will run.

3. Poll phase is the part of the Node.js eventloop where it checks whether asynchronous I/O operation is done. Like file reading and db operations. this pahse also waits for new I/O events when there is no other operations happened.

* Because Node.js gives a higher-priority queue. Node first executes all functions in the nextTick queue before processing microtask queue.

* Node.js uses single threaded event loop ,so I heavy sync operation can delay the other and reduce the performance.